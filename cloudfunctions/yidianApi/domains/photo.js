const { notImplemented } = require("../common/errors");

module.exports = {
  createPhotoUploadSlots() {
    throw notImplemented("photo.createPhotoUploadSlots");
  },

  getPhotoTempUrls() {
    throw notImplemented("photo.getPhotoTempUrls");
  }
};
